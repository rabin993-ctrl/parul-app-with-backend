/**
 * Generated Supabase database types.
 *
 * This is a PLACEHOLDER until the schema exists. After applying migrations and linking
 * the project, regenerate the real types with:
 *
 *   npm run gen:types
 *   # (runs: supabase gen types typescript --linked > src/lib/db-types.ts)
 *
 * Until then `Database` is `any` so the client still compiles.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
